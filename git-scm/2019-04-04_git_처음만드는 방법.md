## computer에 있는 local file들을 github에 처음에 올리는 방법
```
mkdir [directory]
cd [directory]
git init
git remote add origin [repository of github site]
git add --all

git status
git commit -m "[comment]"
git push -u origin master
```
